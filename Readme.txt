A game of chess using unicode characters. 
	This is mostly for programming practice over the following topics:
		
		a simple chess AI
		using data structures that are efficient so the AI isn't slow
		using a user interface that doesn't require hand coding the coordinates

compile with -lncursesw flag
